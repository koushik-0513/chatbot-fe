export const CURRENT_VIEW = ["list", "collection", "article"] as const;
export const SENDER = ["user", "assistant"] as const;
export const STATUS = [
  "processing",
  "processed",
  "unprocessed",
  "failed",
] as const;
